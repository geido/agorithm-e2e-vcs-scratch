# Agorithm E2E VCS scratch repo

Throwaway repo, safe to delete. Used to test Shortcut story 118033's VCS auto-link (branches/pull_requests fields).

Used to test Agorithm's Bug Backlog sweep flow (Diego-owned fixture).
